# twist_mux configuration
# Note: diff_drive_controller parameters belong in helmoro_control/config/helmoro_controller.yaml,
# NOT here. Topics are configured via launch arguments in common_launch.py.
twist_mux:
  ros__parameters: {}
